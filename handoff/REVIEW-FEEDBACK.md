# Review Feedback — Step 3
Date: 2026-05-10
Status: APPROVED

## Conditions
(none — every spec item, constraint, and security check passed.)

## Escalate to Architect
- `scripts/verify_seed_urls.py` 는 brief 에 명시되지 않은 신설 디렉토리(`scripts/`)에 추가됨 — 1회용 검증 유틸리티로 production import 경로 밖에 위치하고 운영 동작에는 영향 없음. Builder Self-Review §5 와 BUILD-LOG 「Step 3 사이트 검증 결과」에 명시되어 있음. 향후 `scripts/` 가 정식 utility 디렉토리로 승격될지 (혹은 1회 사용 후 폐기할지) Architect 결정 요청.
- `Site.status='delayed'` (1~4회 실패) 도입은 brief 명시 없음. Builder Self-Review §3 / Deviation §3 에 등재. 기존 enum (`ok|delayed|failed|blocked`) 안에서 의미 충돌은 없으나 UI 표기 정책은 Step 4 Notifier 단계에서 결정 필요.
- `tests/test_watchtower_crawler.py:test_worker_domain_lock` 의 `b_seen_before_release = (stub_b._result.duration_ms != 0)` 은 stub 의 duration_ms 가 항상 0 이라 사실상 `False` 단정만 남는 약한 검증 — 실제 lock 행위가 깨져도 테스트는 통과. Builder Self-Review §1 에 동일 우려 등재. brief §3.11 은 "직렬화 확인" 만 요구하고 강한 ordering 단정은 요구하지 않으므로 Condition 으로 올리지 않았으나, Step 4 이후 multi-domain 테스트 강화 시 함께 보강 권고. 정책 결정(현 약한 검증 그대로 둘지 / 다음 Step 에서 보강할지) Architect 판단 부탁.

## Cleared
`monitor/watchtower/{base,robots,rss,html,detector,worker}.py` 신규 패키지, `app/db/models.py`(Item.id default + Site.enabled), `app/db/seed.py`(enabled 반영), `config/seed_sites.yaml`(14 verified / 1 보정 / 15 disabled + 주석 형식 일치), `main.py` lifespan(WatchtowerWorker 인스턴스화 + enabled 사이트만 add_job + 부팅 로그 + shutdown 훅 + `POST /api/trigger-watchtower` 202 + BackgroundTasks), `requirements.txt`(httpx 상한 + bs4 + lxml), `scripts/verify_seed_urls.py`, `tests/test_watchtower_crawler.py`(21 hermetic 테스트) 모두 검토 — 브리프 §3.1~3.11 14개 항목 전부 충족, Constraints(Step 1·Step 2 자산 무파괴, 외부 LLM 없음, httpx timeout 30s, CHANGE 미구현, 신규 의존성 3개) 전부 준수, FR-CRL-005~008 / FR-DET-001~002 / FR-SITE-005~006 spec anchor 일치, `pytest tests/ -v` 로컬 재현 결과 **104 passed in 3.28s, 0 skipped** (Builder 주장과 동일), 보안 검토(BS4 XSS — 저장만 하고 render 는 Step 2 `escapeHtml` 책임 / SSRF — `_is_useful_href` + `parsed.scheme.startswith("http")` 로 `file:`/`javascript:`/`mailto:`/`tel:` 차단 / robots.txt fetch fail-open 정책 brief Decision §2 합치 / ThreadPoolExecutor 모든 mutation `_in_progress_lock`·`_counters_lock`·`_domain_locks_master` 보호 / lifespan teardown 에서 `watchtower_worker.shutdown(wait=False)` 호출 / `POST /api/trigger-watchtower` site_id 는 PK 조회만으로 SSRF 표면 없음 / `User-Agent = "Watchtower/1.0 (+https://watchtower.shinhan.local)"` 모든 outbound 헤더 일치 / DEFAULT_TIMEOUT_SEC=30 모든 httpx 호출 적용 / HtmlCrawler max_redirects=5 명시) 통과, 부팅 로그 형식(`Watchtower scheduler: %d sites registered (%d skipped — disabled)`) brief Flag §4 그대로, yaml 주석 형식(`# verified 2026-05-10` / `# tentative — pending owner review`) brief Flag §1 그대로, BUILD-LOG 사이트 검증 결과 표 등재. Step 1 자산(`monitor/collectors/`·`monitor/worker.py`·`monitor/notifier.py`·`monitor/matcher.py`·`app/database.py`·`app/scheduler.py`·`app/routes/status.py`) `git diff` 비어있음, Step 2 자산(`app/routes/watchtower.py` 5개 GET endpoint) 무수정 확인. 머지 가능.
